# Working script commenting fro testing ui initiated build 26-11-25

# name: Update Angular UI Tokens

# on:
#   push:
#     branches: [ main ]
#     paths:
#       - '**/*.json'

# jobs:
#   update-ui:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout design tokens
#         uses: actions/checkout@v4

#       - name: Clone Angular UI repo
#         run: |
#           git clone https://ashishsg444:${{ secrets.ANGULAR_UI_DEPLOY_TOKEN }}@github.com/ashishsg444/ui.git
#           cd ui
#           git config user.name "GitHub Actions"
#           git config user.email "actions@github.com"

#       - name: Copy tokens into Angular repo
#         run: |
#           mkdir -p ui/angular-git-int/tokens
#           cp core.json ui/angular-git-int/tokens/core.json

#       - name: Setup Node
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20

#       - name: Install dependencies in UI repo
#         run: |
#           cd ui/angular-git-int
#           npm install

#       - name: Run build:tokens
#         run: |
#           cd ui/angular-git-int
#           npm run build:tokens
#         # This will generate CSS/SCSS files from your tokens

#       - name: Commit and push changes
#         run: |
#           cd ui
#           git checkout -b update-design-tokens-${{ github.run_id }}
#           git add .
#           git commit -m "chore: update design tokens and generated CSS" || echo "No changes to commit"
#           git push https://ashishsg444:${{ secrets.ANGULAR_UI_DEPLOY_TOKEN }}@github.com/ashishsg444/ui.git HEAD:update-design-tokens-${{ github.run_id }}

#       - name: Create Pull Request in UI repo
#         uses: actions/github-script@v7
#         with:
#           github-token: ${{ secrets.ANGULAR_UI_DEPLOY_TOKEN }}
#           script: |
#             await github.rest.pulls.create({
#               owner: 'ashishsg444',
#               repo: 'ui',
#               title: 'chore: update design tokens',
#               head: `update-design-tokens-${process.env.GITHUB_RUN_ID}`,
#               base: 'main',
#               body: 'This PR updates design tokens and regenerates CSS/SCSS files using build:tokens.'
#             })
